distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.0.0-milestone-8-bin.zip
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
# Enables AndroidX support (required for androidx.* libraries)
android.useAndroidX=true

# Enables Jetifier for migrating support libraries to AndroidX automatically
android.enableJetifier=true

# (Optional) JVM options for faster builds
org.gradle.daemon=true
org.gradle.parallel=true

# (Optional) Treat all warnings as errors
# warningsAsErrors=true
